parameters:
  kapitan:
    compile:
      - input_paths:
          - openshift4-console/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - openshift4-console/component/main.jsonnet
        input_type: jsonnet
        output_path: openshift4-console/
      - input_paths:
          - openshift4-console/component/links.jsonnet
        input_type: jsonnet
        output_path: openshift4-console/links/
